{
  "buildCommand": "npm run build",
  "installCommand": "npm install --legacy-peer-deps && npm install -D tailwindcss@latest postcss@latest autoprefixer@latest tailwindcss-animate@latest && npm install",
  "framework": "nextjs",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
} 