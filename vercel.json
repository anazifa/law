{
  "buildCommand": "npm install --legacy-peer-deps && npm install -D tailwindcss postcss autoprefixer && npm run build",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "nextjs",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ]
} 