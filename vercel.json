{
  "buildCommand": "npm install --legacy-peer-deps && npm install -D tailwindcss@latest postcss@latest autoprefixer@latest && npm run build",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "nextjs",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ]
} 